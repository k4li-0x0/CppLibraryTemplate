#pragma once
#include <stdint.h>

static const char *PROJECT_NAME           = "${CMAKE_PROJECT_NAME}";
static const char *PROJECT_VERSION        = "${CMAKE_PROJECT_VERSION}";
const uint32_t     PROJECT_VERSION_MAJOR  = ${CMAKE_PROJECT_VERSION_MAJOR};
const uint32_t     PROJECT_VERSION_MINOR  = ${CMAKE_PROJECT_VERSION_MINOR};
const uint32_t     PROJECT_VERSION_PATCH  = ${CMAKE_PROJECT_VERSION_PATCH};
constexpr uint32_t PROJECT_VERSION_PACKED = PROJECT_VERSION_MAJOR << 22 | PROJECT_VERSION_MINOR << 12 | PROJECT_VERSION_PATCH;
